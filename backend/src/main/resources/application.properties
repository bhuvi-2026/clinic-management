# ==============================================================================
# 1. LOCAL TESTING CONFIGURATION (ACTIVE)
# ==============================================================================
spring.datasource.url=jdbc:postgresql://localhost:5432/thyronex_lab
spring.datasource.username=postgres
spring.datasource.password=Bhuvana@2026

# Hibernate / JPA (Auto-creates all tables in Thyronex_lab and runs data.sql safely)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Logging
logging.file.name=logs/DoctorBooking.log
logging.level.root=INFO

# Mail SMTP Configuration (Local Active)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=blrrudresh972@gmail.com
spring.mail.password=bjvkzeslsfrvkacq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# External Services (Local Active)
whatsapp.api.url=http://localhost:3000
business.email=blrrudresh972@gmail.com
lab.owner.email=u30378592@gmail.com
lab.owner.whatsapp=919513951931

# Admin Security PIN/Password for Admin Portal
admin.secret.key=ThyronexAdmin@2026

# SMS Gateway Configuration (Local Active - Simulated via terminal)
sms.gateway.enabled=false
sms.gateway.api.key=YOUR_LOCAL_TEST_KEY
# Allow large PDF Base64 payloads in JSON POST requests
server.tomcat.max-http-form-post-size=50MB
server.tomcat.max-swallow-size=50MB
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB


# ==============================================================================
# 2. PRODUCTION READY CONFIGURATION (COMMENTED OUT FOR AWS DEPLOYMENT)
# When deploying to AWS, uncomment this block and set these variables in AWS RDS / ECS
# ==============================================================================
# spring.datasource.url=${DATABASE_URL:jdbc:postgresql://thyronex-prod-db.c123456.ap-south-1.rds.amazonaws.com:5432/Thyronex_lab}
# spring.datasource.username=${DATABASE_USERNAME:postgres}
# spring.datasource.password=${DATABASE_PASSWORD:YourStrongAwsRdsPassword2026}
# spring.jpa.hibernate.ddl-auto=update
# spring.sql.init.mode=never
# spring.mail.host=${SMTP_HOST:smtp.gmail.com}
# spring.mail.port=${SMTP_PORT:587}
# spring.mail.username=${SMTP_USERNAME:notifications@thyronexcare.com}
# spring.mail.password=${SMTP_PASSWORD:your_production_google_app_password}
# whatsapp.api.url=${WHATSAPP_API_URL:http://localhost:3000}
# business.email=${BUSINESS_EMAIL:contact@thyronexcare.com}
# lab.owner.email=${LAB_OWNER_EMAIL:owner@thyronexcare.com}
# lab.owner.whatsapp=${LAB_OWNER_WHATSAPP_NUMBER:918088073507}
# admin.secret.key=${ADMIN_SECRET_KEY:ThyronexSuperAdmin@2026#Secure}
# sms.gateway.enabled=true
# sms.gateway.api.key=${FAST2SMS_API_KEY:YOUR_FAST2SMS_PRODUCTION_API_KEY}